# Revenium Configuration (Required)
REVENIUM_METERING_API_KEY=hak_your_key_here
REVENIUM_METERING_BASE_URL=https://api.revenium.ai
REVENIUM_DEBUG=false

# OpenAI Configuration (Required for OpenAI)
OPENAI_API_KEY=sk-your_key_here

# Azure OpenAI Configuration (Optional - for Azure OpenAI)
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/
AZURE_OPENAI_API_KEY=your_azure_key_here
AZURE_OPENAI_DEPLOYMENT=gpt-4o
AZURE_OPENAI_API_VERSION=2024-12-01-preview

# Logging (Optional)
REVENIUM_LOG_LEVEL=INFO

# ============================================================================
# Selective Metering (Optional)
# ============================================================================
# Enable selective metering to only meter functions decorated with @revenium_meter()
# When disabled (default), ALL OpenAI API calls are automatically metered
# When enabled, ONLY calls inside @revenium_meter() decorated functions are metered
#
# Accepted values: "true", "1", "yes", "on" (case-insensitive)
# Default: false (all calls metered - backward compatible)
#
# REVENIUM_SELECTIVE_METERING=false

# ============================================================================
# Trace Visualization Fields (Optional)
# ============================================================================
# These fields enhance trace visualization and analytics in the Revenium dashboard.
# All fields are optional - if not set, they will be omitted from the metering data.
#
# IMPORTANT: Choose the right approach for each field in your project:
# - Use ENV VARS for: Static, deployment-wide values (environment, region, credential_alias)
# - Use usage_metadata for: Dynamic, per-request values (retry_number, parent_transaction_id)
# - Either works for: Semi-static values (trace_type, trace_name, transaction_name)

# Environment name (e.g., 'production', 'staging', 'development')
# Fallbacks: ENVIRONMENT, DEPLOYMENT_ENV
# REVENIUM_ENVIRONMENT=production

# Cloud region (e.g., 'us-east-1', 'eastus', 'us-central1')
# Fallbacks: AWS_REGION, AWS_DEFAULT_REGION, AZURE_REGION, GCP_REGION, GOOGLE_CLOUD_REGION
# REVENIUM_REGION=us-east-1

# Human-readable credential identifier (e.g., 'prod-openai-key', 'team-api-key')
# Helps track credential rotation and security auditing
# REVENIUM_CREDENTIAL_ALIAS=prod-openai-key
